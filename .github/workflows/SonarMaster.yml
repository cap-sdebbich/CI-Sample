name: Sonar Master

on:
  push:
    branches: [ master ]

  workflow_dispatch:

jobs:
  Test:
    runs-on: ubuntu-latest
    steps:
      - name: Create Awesome Badge
        uses: schneegans/dynamic-badges-action@v1.0.0
        with:
          auth: ${{ secrets.GIST_SECRET }}
          gistID: 1ae599bb82893febf6a8c1743711a40b
          filename: shields.json
          label: Hello
          message: World
          color: orange
